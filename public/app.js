
//JAVASCRIPT FOR: deleteGift.html

//console.log('Testing app.js 1 2 3');

/* document.getElementById('addGuestsForm').addEventListener('submit', (e) => {
  e.preventDefault();
  //console.log(e.target.guestFName.value);
  let body = {
    first_name: e.target.guestFName.value, 
    last_name: e.target.guestLName.value, 
    email: e.target.guestEmail.value
  }
  fetch('/api/admin/profile/:id/addedGuest', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ body })
  })
    .then((res) => res.json())
    .then((data) => {
      console.log(data);
      if (data.error) {
        alert(data.error)
      } else {
        location.reload();
      }
    }).catch((err) => {
      console.log(err);
    })

}) */








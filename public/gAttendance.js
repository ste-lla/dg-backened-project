
//javascript to edit a guest (from admin guest attending list)

/* console.log('adminProfile links to gAttendance js');

document.getElementById('gAttending').addEventListener('click', (e) => {
    console.log('I clicked guest attending link');
    console.log(e.target);
    e.preventDefault();

    fetch('----', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({  })
    })
      .then((res) => res.json())
      .then((data) => {
        console.log(data);
        if (data.error) {
          alert(data.error)
        } else {
          location.reload();
        }
      }).catch((err) => {
        console.log(err);
      })
}) */